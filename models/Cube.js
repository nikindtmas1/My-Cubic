class Cube {
    constructor(cubeId, name, description, imageUrl, difficulty){
        this.cubeId = cubeId,
        this.name = name,
        this.description = description,
        this.imageUrl = imageUrl,
        this.difficulty = difficulty
    }
}

module.exports = Cube;